validator.invalid_redirect_uri              = should be valid absolute URI without fragment, but got: ${validatedValue}
validator.missing_redirect_uri              = when 'authorized_grant_types' contains 'auth_code', then you have to provide at least one 'redirect_uri'
validator.invalid_grant_type                = should be valid Authorization Grand, but got: ${validatedValue}
validator.invalid_scope                     = should be valid scope-token according to RFC 6749, but got: ${validatedValue}
javax.validation.constraints.Size.message   = size must be between {min} and {max}, but got: ${validatedValue}
