<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:sec="http://www.springframework.org/schema/security"
       xmlns:oauth="http://www.springframework.org/schema/security/oauth2"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
       http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd
       http://www.springframework.org/schema/security/oauth2 http://www.springframework.org/schema/security/spring-security-oauth2-1.0.xsd">

    
    <oauth:expression-handler id="oauthExpressionHandler" />

    <oauth:web-expression-handler id="oauthWebExpressionHandler" />
    
    <sec:http pattern="${token.endpoint}"
              create-session="stateless"
              entry-point-ref="oauthAuthenticationEntryPoint"
              access-decision-manager-ref="accessDecisionManager"
              authentication-manager-ref="clientAuthenticationManager">

        <sec:intercept-url pattern="${token.endpoint}" access="IS_AUTHENTICATED_FULLY" />
        <sec:anonymous enabled="false" />
        <sec:http-basic entry-point-ref="oauthAuthenticationEntryPoint" />
        <!-- include this only if you need to authenticate clients via request parameters -->
        <sec:custom-filter ref="clientCredentialsTokenEndpointFilter" before="BASIC_AUTH_FILTER" />
        <sec:access-denied-handler ref="oauthAccessDeniedHandler" />
    </sec:http>
    
    <!-->>>>>>>>>>>>>>>>>>>>>>>>>>>>>>> sould be placed in api-security but then it won't work-->
    
    <!-- http://static.springsource.org/spring-security/site/docs/3.0.x/reference/el-access.html -->
    
    <!-- Resources API Security -->
    
     <sec:http pattern="/api/v1/resources/**" create-session="stateless"
              entry-point-ref="oauthAuthenticationEntryPoint" use-expressions="true">
        <sec:intercept-url pattern="/api/v1/resources/**" 
                           access="hasRole('ROLE_BIG_BOSS') and #oauth2.hasScope('manager')"
                           method="PUT" />
        <sec:intercept-url pattern="/api/v1/resources/**" 
                           access="hasRole('ROLE_BIG_BOSS') and #oauth2.hasScope('manager')"
                           method="POST" />
        <sec:intercept-url pattern="/api/v1/resources/**" 
                           access="hasRole('ROLE_BIG_BOSS') and #oauth2.hasScope('manager')"
                           method="DELETE" />
        <sec:intercept-url pattern="/api/v1/resources/**"
                           access="permitAll"
                           method="GET"/>
        <sec:custom-filter ref="resourceServerFilterResourcesApi" before="PRE_AUTH_FILTER" />
        <sec:access-denied-handler ref="oauthAccessDeniedHandler" />
        <sec:expression-handler ref="oauthWebExpressionHandler" />
    </sec:http>
    
    <!-- Clients API Security -->

    <sec:http pattern="/api/v1/clients/**" create-session="stateless"
              entry-point-ref="oauthAuthenticationEntryPoint" use-expressions="true">
        <sec:intercept-url pattern="/api/v1/clients/**" 
                           access="hasRole('ROLE_BIG_BOSS') and #oauth2.hasScope('manager')"
                           method="PUT" />
        <sec:intercept-url pattern="/api/v1/clients/**" 
                           access="hasRole('ROLE_BIG_BOSS') and #oauth2.hasScope('manager')"
                           method="POST" />
        <sec:intercept-url pattern="/api/v1/clients/**" 
                           access="hasRole('ROLE_BIG_BOSS') and #oauth2.hasScope('manager')"
                           method="DELETE" />
        <sec:intercept-url pattern="/api/v1/clients/**"
                           access="hasRole('ROLE_BIG_BOSS') and #oauth2.hasScope('manager')"
                           method="GET"/>
        <sec:custom-filter ref="resourceServerFilterClientsApi" before="PRE_AUTH_FILTER" />
        <sec:access-denied-handler ref="oauthAccessDeniedHandler" />
        <sec:expression-handler ref="oauthWebExpressionHandler" />
    </sec:http>
    
    <!-- Check Token Endpoint Security -->
    
    <sec:http pattern="/check-token/**" create-session="stateless"
                  entry-point-ref="oauthAuthenticationEntryPoint" use-expressions="true">
        <sec:intercept-url pattern="/check-token/**" 
                           access="hasAnyRole('ROLE_BIG_BOSS','ROLE_RESOURCE_SERVER') and #oauth2.hasScope('token-check')"
                           method="GET" />
        <sec:custom-filter ref="resourceServerFilterCheckTokenEndpoint" before="PRE_AUTH_FILTER" />
        <sec:access-denied-handler ref="oauthAccessDeniedHandler" />
        <sec:expression-handler ref="oauthWebExpressionHandler" />
    </sec:http>
    
    <oauth:resource-server id="resourceServerFilterResourcesApi" token-services-ref="tokenServices" resource-id="auth-server-resources-api" />
    
    <oauth:resource-server id="resourceServerFilterClientsApi" token-services-ref="tokenServices" resource-id="auth-server-clients-api" />
    
    <oauth:resource-server id="resourceServerFilterCheckTokenEndpoint" token-services-ref="tokenServices" resource-id="auth-server-check-token-endpoint" />
    
    <!--^^^^^^^^^^^^^^^^^^^^ sould be placed in api-security but then it won't work-->
    
    
    <!-- element je zakomentovany, lebo vacsinu z tych veci moze spring generovat automaticky, co v tomto stadiu postacuje...
    following section is temporary commented bock, spring generates most of that stuff automatically, which is sufficient at this stage.....
    -->
    <sec:http access-denied-page="/login.jsp?authorization_error=true"
              disable-url-rewriting="true"
              access-decision-manager-ref="accessDecisionManager">

        <sec:intercept-url pattern="/oauth/**" access="IS_AUTHENTICATED_FULLY" />
        <sec:intercept-url pattern="/**" access="IS_AUTHENTICATED_ANONYMOUSLY" />
        <sec:form-login login-page="/login.jsp"
                        login-processing-url="/login.do"
                        default-target-url="/index.jsp"
                        authentication-failure-url="/login.jsp?authentication_error=true" />
        <sec:logout logout-url="/logout.do" logout-success-url="/index.jsp" />
        <sec:anonymous />
    </sec:http>


    <bean id="accessDecisionManager" class="org.springframework.security.access.vote.UnanimousBased">
        <constructor-arg>
            <list>
                <bean class="org.springframework.security.oauth2.provider.vote.ScopeVoter" />
                <bean class="org.springframework.security.access.vote.RoleVoter" />
                <bean class="org.springframework.security.access.vote.AuthenticatedVoter" />
            </list>
        </constructor-arg>
    </bean>

    <!--
        Client authentication with HTTP Basic scheme. This is the recommended way by specification.
    -->
    <bean id="oauthAuthenticationEntryPoint" class="org.springframework.security.oauth2.provider.error.OAuth2AuthenticationEntryPoint" />

    <!--
        Filter that supports client authentication with credentials in request body (parameters client_id and client_secret).
        Note: This method of authentication is not recommended by OAuth specification (draft-ietf-oauth-v2-31, 16)!
              Clients shoud use HTTP Basic scheme instead.
    -->
    <bean id="clientCredentialsTokenEndpointFilter" class="org.springframework.security.oauth2.provider.client.ClientCredentialsTokenEndpointFilter">
        <property name="authenticationManager" ref="clientAuthenticationManager" />
    </bean>
    
    <bean id="oauthAccessDeniedHandler" class="org.springframework.security.oauth2.provider.error.OAuth2AccessDeniedHandler" />

    
    <sec:authentication-manager id="clientAuthenticationManager">
        <sec:authentication-provider user-service-ref="clientDetailsUserService" />
    </sec:authentication-manager>
    
    <bean id="clientDetailsUserService" class="org.springframework.security.oauth2.provider.client.ClientDetailsUserDetailsService">
        <constructor-arg ref="clientDetails" />
    </bean>


    <!-- TO DELETE (only for testing) -->

    <sec:authentication-manager alias="authenticationManager">
        <sec:authentication-provider>
            <sec:user-service>
                <sec:user name="tomy" password="best" authorities="ROLE_USER" />
            </sec:user-service>
        </sec:authentication-provider>
    </sec:authentication-manager>
    
</beans>
