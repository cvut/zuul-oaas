<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:oauth="http://www.springframework.org/schema/security/oauth2"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
       http://www.springframework.org/schema/security/oauth2 http://www.springframework.org/schema/security/spring-security-oauth2-1.0.xsd">
    
    <!--
        Authorization server specifies that the OAuth2 authorization and token endpoints should
        be created in the application context.
    -->
    <oauth:authorization-server
        authorization-endpoint-url="${oaas.endpoint.authorization}"
        token-endpoint-url="${oaas.endpoint.token}"
        client-details-service-ref="clientDetailsService"
        token-services-ref="tokenServices"
        user-approval-handler-ref="clientApprovalHandler">

        <oauth:authorization-code />
        <oauth:implicit />
        <oauth:refresh-token />
        <oauth:client-credentials />
        <oauth:password disabled="true" />
    </oauth:authorization-server>

</beans>
