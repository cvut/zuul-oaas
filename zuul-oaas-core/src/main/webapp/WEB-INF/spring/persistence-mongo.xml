<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
       http://www.springframework.org/schema/data/mongo http://www.springframework.org/schema/data/mongo/spring-mongo-1.0.xsd">

    
    <!--////////// Mongo DB Persistence //////////-->


    <!-- Define MongoDB instance -->
    <mongo:mongo host="${persistence.mongo.host}" port="${persistence.mongo.port}" />

    <!-- Configure database access -->
    <mongo:db-factory id="mongoDbFactory"
                      dbname="${persistence.mongo.dbname}"
                      username="${persistence.mongo.username}"
                      password="${persistence.mongo.password}"
                      write-concern="SAFE" />

    <!-- Enable JMX MBeans export -->
    <mongo:jmx />


    <!--//// Converters ////-->

    <!-- Register converters for mapping domain objects to Mongo and vice versa -->
    <mongo:mapping-converter id="mappingConverter" db-factory-ref="mongoDbFactory">
        <mongo:custom-converters base-package="cz.cvut.zuul.oaas.repos.mongo.converters" />
    </mongo:mapping-converter>

    <!-- ConversionService to which are converters automatically registered -->
    <bean id="conversionService" class="org.springframework.context.support.ConversionServiceFactoryBean" />


    <!--//// DAOs ////-->

    <!-- Template that provides basic CRUD operations against Mongo database -->
    <bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
        <constructor-arg name="mongoDbFactory" ref="mongoDbFactory" />
        <constructor-arg name="mongoConverter" ref="mappingConverter" />
        <property name="writeResultChecking" value="EXCEPTION" />
    </bean>

    <bean id="clientsRepo" class="cz.cvut.zuul.oaas.repos.mongo.MongoClientsRepo"
          p:mongoOperations-ref="mongoTemplate" />

    <bean id="accessTokensRepo" class="cz.cvut.zuul.oaas.repos.mongo.MongoAccessTokensRepo"
          p:mongoOperations-ref="mongoTemplate" />

    <bean id="refreshTokensRepo" class="cz.cvut.zuul.oaas.repos.mongo.MongoRefreshTokensRepo"
          p:mongoOperations-ref="mongoTemplate" />

    <bean id="resourcesRepo" class="cz.cvut.zuul.oaas.repos.mongo.MongoResourcesRepo"
          p:mongoOperations-ref="mongoTemplate" />


    <!--//// Seeder ////-->

    <beans profile="dev">
        <!-- Insert seed data into database -->
        <bean class="cz.cvut.zuul.oaas.repos.mongo.support.MongoSeedLoader" lazy-init="false"
              init-method="seed"
              p:mongoTemplate-ref="mongoTemplate"
              p:location="classpath:config/seeds.json" />
    </beans>

</beans>
